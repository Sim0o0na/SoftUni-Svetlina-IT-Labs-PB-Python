# Read all input
# Convert input to numbers
b1 = float(input())
b2 = float(input())
h = float(input())

# Calculate trapezoid area by (b1 + b2) * h / 2
# Save area in a variable
trapezoid_area = (b1 + b2) * h / 2

# Print area
print(trapezoid_area)